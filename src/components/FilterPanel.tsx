import React from "react";

const FilterPanel: React.FC = () => {
  return (
    <aside style={{ padding: "1rem", background: "#f0f0f0" }}>
      <h2>Filter Panel</h2>
      <p>Here you can add filtering controls later.</p>
    </aside>
  );
};

export default FilterPanel;
